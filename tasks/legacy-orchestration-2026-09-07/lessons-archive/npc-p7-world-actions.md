# NPC-P7-ACTION-WORLD-001 recovery and review

- Symptom: inherited world tests did not compile because three imports were appended after declarations. Root cause: unfinished patch placement. Prevention: compile the inherited work before treating its tests as evidence. Verification: imports restored to the import block; world focused tests pass.
- Symptom: handoff described GIVEBUFF's optional boolean as visibility. Root cause: trusting the local variable name instead of tracing the actual AddBuff signature and HumanObject override. Prevention: trace each argument through all overrides; visibility comes from BuffInfo, while parameter four controls RefreshStats and parameters three/five are unused. Verification: metadata recipients, stacked/infinite durations, unchecked seconds conversion and deferred removal tests.
- Symptom: DROP parser test indexed the page slice as a map, and its first packet expectation omitted NewItemInfo. Root cause: assumed API shape and incomplete transcript. Prevention: use NPCScript.Page and consume definition packets before item packets. Verification: parser, loader, production DROP and grouped quest/capacity tests pass.
- Symptom: CLEARPETS's inherited next-turn hook skipped archer pets before seeing the flag. Root cause: placing a cross-pet lifecycle flag after a type-specific gate. Prevention: process the shared death flag before subtype dispatch. Verification: focused pet lifecycle tests and broader pet regressions.
